import { ColordirDirective } from './colordir.directive';

describe('ColordirDirective', () => {
  it('should create an instance', () => {
    const directive = new ColordirDirective();
    expect(directive).toBeTruthy();
  });
});
